#include <dt-bindings/zmk/behaviors.h>
#include <dt-bindings/zmk/keys.h>

/ {
    behaviors {
#if ZMK_BEHAVIOR_OMIT(RUEN)
    /omit-if-no-ref/
#endif
        ruen: ruen {
            compatible = "zmk,behavior-ruen";
            #binding-cells = <1>;
            lang-id = <1>; // 0 = EN, 1 = RU (пример)
        };
    };
};
